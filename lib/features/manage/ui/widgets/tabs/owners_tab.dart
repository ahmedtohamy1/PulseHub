import 'package:flutter/material.dart';

class OwnersTab extends StatelessWidget {
  const OwnersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Owners Tab Content'),
    );
  }
}
