import 'package:flutter/material.dart';

class ManagedUsersTab extends StatelessWidget {
  const ManagedUsersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Managed Users Tab Content'),
    );
  }
}
