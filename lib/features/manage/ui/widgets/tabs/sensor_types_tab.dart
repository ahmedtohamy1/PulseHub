import 'package:flutter/material.dart';

class SensorTypesTab extends StatelessWidget {
  const SensorTypesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sensor Types Tab Content'),
    );
  }
}
